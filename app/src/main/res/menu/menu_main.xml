<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:app="http://schemas.android.com/apk/res-auto" xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:id="@+id/menu_refresh" android:title="새로고침"
          android:icon="@android:drawable/stat_notify_sync"
          app:showAsAction="always"/>

    <item
            android:id="@+id/menu_search"
            android:title="검색"
            android:orderInCategory="102"
            app:showAsAction="always|withText" android:icon="@android:drawable/ic_menu_search"
        app:actionLayout="@layout/search_layout"/>

    <item
            android:id="@+id/menu_settings"
            android:title="설정"
            app:showAsAction="always" android:icon="@android:drawable/ic_menu_preferences"/>
</menu>